<!doctype html>
<html lang="en">
<head>
</head>
<body>
<div style="margin: 0; padding: 0px;">
	 <p>${staticContentsMap['candidate.suite.invitation.email.dear']} ${name},</p>
	 <p style="margin-left: 120px;">${staticContentsMap['candidate.suite.forgot.email.message']}  <a href="${serverUrl}${homeUrl}?token=${token}" target="_blank" style="color:#5443d5; font-weight: bold; text-decoration:none;">
			          		${serverUrl}
			          	</a> </p>
	<p>${staticContentsMap['candidate.suite.invitation.email.sincerely']} ${staticContentsMap['candidate.suite.email.sincerely.admin']}</p>
	
</div>
</body>
</html>